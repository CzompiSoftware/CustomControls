<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:hu.czompisoftware.customcontrols.CuttingCorners">

    <!--#region LightGray -->
    <Style TargetType="{x:Type local:DragAndDrop}" x:Key="DragAndDropLightGray">
        <Setter Property="Background" Value="#EAEAEA"/>
        <Setter Property="DogEar" Value="8"/>
        <Setter Property="AllowDrop" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DragAndDrop}">
                    <Border CornerRadius="0"
                            BorderThickness="0"
                            Background="{TemplateBinding Background}">
                        <ContentPresenter Content="{TemplateBinding Content}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value=".65"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <!--<Style TargetType="{x:Type local:DragAndDrop}" x:Key="Default" BasedOn="{StaticResource ResourceKey=Primary}"/>-->
    <Style TargetType="{x:Type local:DragAndDrop}" BasedOn="{StaticResource ResourceKey=DragAndDropLightGray}"/>
</ResourceDictionary>